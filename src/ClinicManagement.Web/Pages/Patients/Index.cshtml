@page
@model ClinicManagement.Web.Pages.Patients.IndexModel
@{
    ViewData["Title"] = "Patients";
}

<h1>Patients</h1>

<p>
    <a asp-page="Create" class="btn btn-primary">Create New Patient</a>
</p>

<form method="get" class="mb-3">
    <div class="row">
        <div class="col-md-6">
            <input type="text" name="searchTerm" value="@Model.SearchTerm" class="form-control" placeholder="Search patients..." />
        </div>
        <div class="col-md-2">
            <button type="submit" class="btn btn-secondary">Search</button>
        </div>
    </div>
</form>

<table class="table table-striped">
    <thead>
        <tr>
            <th>ID</th>
            <th>Name</th>
            <th>Email</th>
            <th>Phone</th>
            <th>Age</th>
            <th>Gender</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var patient in Model.Patients)
        {
            <tr>
                <td>@patient.PatientID</td>
                <td>@patient.Name</td>
                <td>@patient.Email</td>
                <td>@patient.Phone</td>
                <td>@patient.Age</td>
                <td>@patient.Gender</td>
                <td>
                    <a asp-page="Details" asp-route-id="@patient.PatientID" class="btn btn-sm btn-info">Details</a>
                    <a asp-page="Edit" asp-route-id="@patient.PatientID" class="btn btn-sm btn-warning">Edit</a>
                    <a asp-page="Delete" asp-route-id="@patient.PatientID" class="btn btn-sm btn-danger">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
